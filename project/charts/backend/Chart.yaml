apiVersion: v2
name: backend
description: Student management backend (Flask API) for EKS
type: application
version: 0.1.0          # chart version - bump when you change the chart structure
appVersion: "1.0.0"     # informational: the app version this chart deploys
